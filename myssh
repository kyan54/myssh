#!/usr/bin/env bash
# =============================================================================
# MySsh - WSL SSH服务器快速连接工具
# 功能：模糊搜索服务器列表，支持 sshpass 连接和 WinSCP 文件管理
# =============================================================================

set -o pipefail

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
SERVERS_FILE="${MYSSH_SERVERS_FILE:-$SCRIPT_DIR/servers.txt}"
WINSCP_EXE="${MYSSH_WINSCP_PATH:-/mnt/c/Users/THINKBOOK/AppData/Local/Programs/WinSCP/WinSCP.exe}"

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
NC='\033[0m' # No Color
BOLD='\033[1m'

# 列宽配置（用于表格对齐）
LABEL_W=20
HOST_W=17
PORT_W=7
USER_W=17

# =============================================================================
# 工具函数
# =============================================================================

print_error() {
    echo -e "${RED}✗ 错误: $1${NC}" >&2
}

print_success() {
    echo -e "${GREEN}✓ $1${NC}"
}

print_info() {
    echo -e "${CYAN}➜ $1${NC}"
}

print_warn() {
    echo -e "${YELLOW}⚠ $1${NC}"
}

# 计算字符串显示宽度（中文占2，英文占1）
display_width() {
    local str="$1"
    local width=0
    local char
    for ((i=0; i<${#str}; i++)); do
        char="${str:i:1}"
        # 检查是否为多字节字符（中文等）
        if [[ $(printf '%s' "$char" | wc -c) -gt 1 ]]; then
            ((width+=2))
        else
            ((width+=1))
        fi
    done
    echo $width
}

# 初始化列宽（根据终端宽度动态调整标签列，避免被截断过多）
init_column_widths() {
    local term_cols
    term_cols=$(tput cols 2>/dev/null || echo 120)
    local content_cols=$((term_cols - 4)) # 预留指示符和边距
    local sep_w=9  # " │ " * 3
    local min_label=16
    local max_label=60

    local label_w=$((content_cols - HOST_W - PORT_W - USER_W - sep_w))
    if ((label_w < min_label)); then
        label_w=$min_label
    elif ((label_w > max_label)); then
        label_w=$max_label
    fi
    LABEL_W=$label_w
}

# 重复字符
repeat_char() {
    local count="$1"
    local char="$2"
    local out=""
    for ((i=0; i<count; i++)); do
        out+="$char"
    done
    printf '%s' "$out"
}

# 生成 fzf 表头
build_fzf_header() {
    local title="🔍 MySsh - 服务器快速连接 (输入关键字搜索, Enter确认, Esc退出)"
    local col_header
    col_header=$(printf "%s │ %s │ %s │ %s" \
        "$(pad_string "标签" "$LABEL_W")" \
        "$(pad_string "服务器IP" "$HOST_W")" \
        "$(pad_string "端口" "$PORT_W")" \
        "$(pad_string "用户名" "$USER_W")")
    local sep_line
    sep_line=$(printf "%s─┼─%s─┼─%s─┼─%s" \
        "$(repeat_char "$LABEL_W" "─")" \
        "$(repeat_char "$HOST_W" "─")" \
        "$(repeat_char "$PORT_W" "─")" \
        "$(repeat_char "$USER_W" "─")")

    printf "%s\n%s\n%s" "$title" "$col_header" "$sep_line"
}

# 截断字符串到指定显示宽度
truncate_string() {
    local str="$1"
    local max_width="$2"
    local width=0
    local char char_w out=""

    for ((i=0; i<${#str}; i++)); do
        char="${str:i:1}"
        if [[ $(printf '%s' "$char" | wc -c) -gt 1 ]]; then
            char_w=2
        else
            char_w=1
        fi
        if (( width + char_w > max_width )); then
            break
        fi
        width=$((width + char_w))
        out+="$char"
    done
    printf '%s' "$out"
}

# 按指定宽度填充字符串
pad_string() {
    local str="$1"
    local target_width="$2"
    local truncated
    truncated=$(truncate_string "$str" "$target_width")
    local current_width
    current_width=$(display_width "$truncated")
    local padding=$((target_width - current_width))
    printf '%s' "$truncated"
    if [[ $padding -gt 0 ]]; then
        printf '%*s' "$padding" ''
    fi
}

# 检查依赖
check_dependencies() {
    local missing=()
    
    if ! command -v sshpass &>/dev/null; then
        missing+=("sshpass")
    fi
    
    if ! command -v fzf &>/dev/null; then
        missing+=("fzf")
    fi
    
    if [[ ${#missing[@]} -gt 0 ]]; then
        print_error "缺少必需的依赖: ${missing[*]}"
        echo "请安装: sudo apt install ${missing[*]}"
        exit 1
    fi
    
    # 检查 WinSCP（仅警告，不阻止运行）
    if [[ ! -f "$WINSCP_EXE" ]]; then
        print_warn "WinSCP 未找到: $WINSCP_EXE"
        echo "      WinSCP 功能 (-w) 将不可用，请安装 WinSCP 或设置 MYSSH_WINSCP_PATH"
    fi
}

# 检查服务器配置文件
check_servers_file() {
    if [[ ! -f "$SERVERS_FILE" ]]; then
        print_error "服务器配置文件不存在: $SERVERS_FILE"
        echo ""
        echo "请创建配置文件，格式如下（每行一个服务器，使用 Tab 分隔）:"
        echo -e "${CYAN}标签名\t服务器IP\t端口\t账号\t密码${NC}"
        echo ""
        echo "示例:"
        echo -e "${CYAN}生产服务器\t192.168.1.100\t22\troot\tpassword123${NC}"
        exit 1
    fi
}

# =============================================================================
# 核心功能
# =============================================================================

# 加载服务器列表
# 支持格式：
#   1. 纯Tab分隔: 标签名\t服务器IP\t端口\t账号\t密码
#   2. 混合分隔: 标签名\t服务器IP 端口 账号 密码
#   3. 纯空格分隔: 标签名 服务器IP 端口 账号 密码
load_servers() {
    local servers=()
    local line label host port user pass
    
    while IFS= read -r line || [[ -n "$line" ]]; do
        # 跳过空行和注释
        [[ -z "$line" || "$line" =~ ^[[:space:]]*# ]] && continue
        
        # 将Tab替换为空格，统一用空格分隔
        line="${line//$'\t'/ }"
        
        # 按空格分隔读取各字段
        read -r label host port user pass <<< "$line"
        
        servers+=("$label|$host|$port|$user|$pass")
    done < "$SERVERS_FILE"
    printf '%s\n' "${servers[@]}"
}

# 格式化显示服务器列表
format_server_list() {
    local index=1
    while IFS='|' read -r label host port user pass; do
        printf "%-3s │ %s │ %s │ %s │ %s\n" \
            "$index" \
            "$(pad_string "$label" "$LABEL_W")" \
            "$(pad_string "$host" "$HOST_W")" \
            "$(pad_string "$port" "$PORT_W")" \
            "$(pad_string "$user" "$USER_W")"
        ((index++))
    done
}

# 使用 fzf 选择服务器
select_server_with_fzf() {
    init_column_widths
    local servers
    servers=$(load_servers)
    
    if [[ -z "$servers" ]]; then
        print_error "服务器列表为空"
        exit 1
    fi
    
    # 创建带格式的显示列表
    local formatted
    local index=1
    formatted=$(echo "$servers" | while IFS='|' read -r label host port user pass; do
        local display
        display=$(printf '%s │ %s │ %s │ %s' \
            "$(pad_string "$label" "$LABEL_W")" \
            "$(pad_string "$host" "$HOST_W")" \
            "$(pad_string "$port" "$PORT_W")" \
            "$(pad_string "$user" "$USER_W")")
        printf '%s\t%s\n' "$display" "$index"
        ((index++))
    done)
    
    # 使用 fzf 进行模糊搜索
    local header
    header=$(build_fzf_header)
    
    local selected_line
    selected_line=$(echo "$formatted" | fzf \
        --header="$header" \
        --height=60% \
        --layout=reverse \
        --border=rounded \
        --prompt=" 搜索: " \
        --pointer="▶" \
        --marker="✓" \
        --color="header:italic:yellow,prompt:green,pointer:cyan,marker:magenta" \
        --preview-window=hidden \
        --bind="enter:accept" \
        --bind="esc:abort" \
        --delimiter=$'\t' \
        --with-nth=1 \
        --nth=1 \
        --no-info)
    
    if [[ -z "$selected_line" ]]; then
        return 1
    fi
    
    # 根据选中的行索引找回完整数据
    local line_index
    line_index=$(printf '%s' "$selected_line" | cut -f2)
    echo "$servers" | sed -n "${line_index}p"
}

# SSH 连接
connect_ssh() {
    local host="$1"
    local port="$2"
    local user="$3"
    local pass="$4"
    
    print_info "正在连接 ${user}@${host}:${port} ..."
    echo ""
    
    sshpass -p "$pass" ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
        -p "$port" "${user}@${host}"
}

# 打开 WinSCP
open_winscp() {
    local host="$1"
    local port="$2"
    local user="$3"
    local pass="$4"
    local remote_path="${5:-/}"
    
    if [[ ! -f "$WINSCP_EXE" ]]; then
        print_error "WinSCP 未找到: $WINSCP_EXE"
        echo "请安装 WinSCP 或设置环境变量 MYSSH_WINSCP_PATH"
        return 1
    fi
    
    # URL 编码密码中的特殊字符
    local encoded_pass
    encoded_pass=$(python3 -c "import urllib.parse; print(urllib.parse.quote('$pass', safe=''))" 2>/dev/null || echo "$pass")
    
    local url="sftp://${user}:${encoded_pass}@${host}:${port}${remote_path}"
    
    print_info "正在打开 WinSCP 连接 ${host}..."
    "$WINSCP_EXE" "$url" >/dev/null 2>&1 &
    print_success "WinSCP 已启动"
}

# 显示帮助
show_help() {
    echo ""
    echo -e "${BOLD}MySsh - WSL SSH服务器快速连接工具${NC}"
    echo ""
    echo -e "${CYAN}用法:${NC}"
    echo "  myssh              交互式选择服务器并 SSH 连接"
    echo "  myssh -w           交互式选择服务器并用 WinSCP 打开"
    echo "  myssh -l           列出所有服务器"
    echo "  myssh -e           编辑服务器配置文件"
    echo "  myssh -h           显示帮助信息"
    echo ""
    echo -e "${CYAN}环境变量:${NC}"
    echo "  MYSSH_SERVERS_FILE  服务器配置文件路径 (默认: $SCRIPT_DIR/servers.txt)"
    echo "  MYSSH_WINSCP_PATH   WinSCP 可执行文件路径"
    echo ""
    echo -e "${CYAN}配置文件格式:${NC}"
    echo "  每行一个服务器，使用 Tab 分隔:"
    echo -e "  ${YELLOW}标签名\t服务器IP\t端口\t账号\t密码${NC}"
    echo ""
    echo -e "${CYAN}示例:${NC}"
    echo -e "  ${YELLOW}生产服务器\t192.168.1.100\t22\troot\tmypassword${NC}"
    echo -e "  ${YELLOW}测试环境\t10.0.0.50\t2222\tadmin\ttest123${NC}"
    echo ""
}

# 列出服务器
list_servers() {
    init_column_widths
    check_servers_file
    
    echo ""
    echo -e "${BOLD}服务器列表${NC} (${SERVERS_FILE})"
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    printf "${CYAN}%-3s │ %s │ %s │ %s │ %s${NC}\n" \
        "#" \
        "$(pad_string "标签" "$LABEL_W")" \
        "$(pad_string "服务器IP" "$HOST_W")" \
        "$(pad_string "端口" "$PORT_W")" \
        "$(pad_string "用户名" "$USER_W")"
    echo "────┼$(repeat_char "$LABEL_W" "─")┼$(repeat_char "$HOST_W" "─")┼$(repeat_char "$PORT_W" "─")┼$(repeat_char "$USER_W" "─")"
    load_servers | format_server_list
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo ""
}

# 编辑配置文件
edit_servers() {
    local editor="${EDITOR:-nano}"
    
    # 如果配置文件不存在，创建模板
    if [[ ! -f "$SERVERS_FILE" ]]; then
        mkdir -p "$(dirname "$SERVERS_FILE")"
        cat > "$SERVERS_FILE" << 'EOF'
# MySsh 服务器配置文件
# 格式: 标签名<TAB>服务器IP<TAB>端口<TAB>账号<TAB>密码
# 注意: 使用 Tab 键分隔各字段，以 # 开头的行为注释
#
# 示例:
#生产服务器	192.168.1.100	22	root	password123
#测试环境	10.0.0.50	2222	admin	test456
EOF
        print_info "已创建配置文件模板: $SERVERS_FILE"
    fi
    
    "$editor" "$SERVERS_FILE"
}

# =============================================================================
# 主程序
# =============================================================================

main() {
    local mode="ssh"  # 默认模式: ssh
    
    # 解析参数
    while [[ $# -gt 0 ]]; do
        case "$1" in
            -w|--winscp)
                mode="winscp"
                shift
                ;;
            -l|--list)
                list_servers
                exit 0
                ;;
            -e|--edit)
                edit_servers
                exit 0
                ;;
            -h|--help)
                show_help
                exit 0
                ;;
            *)
                print_error "未知参数: $1"
                show_help
                exit 1
                ;;
        esac
    done
    
    # 检查依赖和配置
    check_dependencies
    check_servers_file
    
    # 选择服务器
    local selected
    selected=$(select_server_with_fzf)
    
    if [[ -z "$selected" ]]; then
        print_info "已取消"
        exit 0
    fi
    
    # 解析选中的服务器信息
    local label host port user pass
    IFS='|' read -r label host port user pass <<< "$selected"
    
    echo ""
    print_success "已选择: $label ($host)"
    
    # 执行操作
    case "$mode" in
        ssh)
            connect_ssh "$host" "$port" "$user" "$pass"
            ;;
        winscp)
            open_winscp "$host" "$port" "$user" "$pass"
            ;;
    esac
}

main "$@"
